[{:db/id -1, :genre/id :stealth, :genre/status :implemented, :genre/core-loop ["\"guard\" entities patrol/drift, matching the ai-tick move-toward pattern\n   but inverted: player must AVOID being the target they close in on" "a defatom alert counter rises while a guard is within detection range,\n   decays otherwise" "reaching a fixed \"goal\" entity while alert stays below a threshold wins\n   the round; alert maxing out fails it and resets"], :genre/controls "{:buttons 1, :scheme [\"movement only, avoidance is positional\"]}", :genre/kami-engine "{:proven-vocab-used [:nearest-tagged :defatom :set-atom! :< :move-toward! :get-x :get-y], :known-constraints [\"no line-of-sight/occlusion primitive exists -- detection is pure\\n    Euclidean range via nearest-tagged, not vision-cone based\"]}", :games/dir "games/stealth"}]