[{:db/id -1, :genre/id :fighting, :genre/status :design-only, :genre/core-loop ["1 human + 1 AI opponent (same input-stream constraint as\n   local-multiplayer), each with a defatom health counter; proximity-based\n   exchanges (nearest-tagged within a fixed range) decrement the opponent's\n   health on a tick-gated interval; first to zero loses, then both reset"], :genre/controls "{:buttons 1, :scheme [\"movement + auto-attack-in-range, single input stream\"]}", :genre/kami-engine "{:proven-vocab-used [:defatom :set-atom! :nearest-tagged :get-x :get-y :tick-n :mod], :known-constraints [\"same single-local-input-stream constraint as local-multiplayer.edn --\\n    true 2-player local fighting is not currently buildable, this is the\\n    1-human+1-AI honest fallback\"]}", :games/dir "games/fighting"}]