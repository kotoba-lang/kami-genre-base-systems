;; schema.edn — Datomic/Datascript 互換スキーマ定義（自動生成 by edn-datomize.bb）
;; :db/ident 属性定義のリスト。Datomic 固有キー(:db.install/_attribute 等)は使わない。
;; 手編集禁止 — 再生成すると上書きされる。
;;
;; wrap-map-keep-ns で変換したファイルは、元々 Clojure 慣用の名前空間付き
;; キーワード(:register/type, :case/id 等)をそのまま保持し、裸キーだけ
;; ファイル(ディレクトリ)単位の namespace を付与している。同じ属性名でも
;; 由来ファイルによって非scalar値の実データ形が異なる場合があるが、値は
;; すべて pr-str の "blob" (valueType=string) として保持しているため型は
;; 常に一意 — ADR 系(90-docs/adr/*.edn)のような型不一致の問題はここでは生じない。

[#:db{:ident :games/dir, :valueType :db.type/string, :cardinality :db.cardinality/one} #:db{:ident :genre/controls, :valueType :db.type/string, :cardinality :db.cardinality/one} #:db{:ident :genre/core-loop, :valueType :db.type/string, :cardinality :db.cardinality/many} #:db{:ident :genre/id, :valueType :db.type/keyword, :cardinality :db.cardinality/one} #:db{:ident :genre/kami-engine, :valueType :db.type/string, :cardinality :db.cardinality/one} #:db{:ident :genre/note, :valueType :db.type/string, :cardinality :db.cardinality/one} #:db{:ident :genre/status, :valueType :db.type/keyword, :cardinality :db.cardinality/one}]
